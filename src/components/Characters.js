import React from 'react';

export default () => (
  <section>
    <h2>Characters</h2>
    <p>Game of Thrones wouldn't be the same without the rich, detailed, and likeable characters that it offers. That's why we're so mad/happy when characters get killed. Click on a name to find out more information.</p>
  </section>
);
